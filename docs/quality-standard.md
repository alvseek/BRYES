# Quality Standard

## Project Info

- **Project**: BRYES (vision-based computer-use agent)
- **Date Generated**: 2026-07-16
- **Generated By**: claude-software-architect
- **Tech Stack**: Python 3.14 (stdlib-first) + Pydantic + Pillow; Flask + Docker (Screen container); OpenRouter (rented Brain/Eyes models); Xvfb/xdotool; ADB (PhoneDevice)

---

## 1. Error Handling

- [ ] Model/perception calls DEGRADE, never crash: `box()` returns `None` (NOT_FOUND / unparseable), `describe()` falls back (box-miss → `VISUAL_FOCUS FAILED` + overview), `decide()` retries rather than raising.
- [ ] Structured LLM failures raise `StructuredError` ([structured.py](structured.py)) carrying the raw provider body (`.body`) — captured to the transcript as a `decide-error` record for root-causing, never silently swallowed.
- [ ] Transient network faults (`URLError` / timeout / reset) on model and device calls are caught and retried, not fatal.
- [ ] Console output is encoding-safe (`loop.run()` reconfigures stdout to utf-8/replace) so uncontrolled VLM text (UI glyphs, em-dashes) can't crash a run.

---

## 2. State Completeness

*Not applicable* — BRYES has no UI of its own. It perceives and drives EXTERNAL UIs via vision; there are no loading/empty/error states to build here.

---

## 3. UX Completeness

*Not applicable* — no UI (see Dimension 2).

---

## 4. Efficiency

- [ ] Model tiering: the fast, cheap model is the default (`describe` = `qwen3-vl-8b`); the expensive 72B is used ONLY for boxing and the `recheck` / `request_diff` escalation rungs.
- [ ] Foveal describe (ADR-004): describe latency ∝ OUTPUT LENGTH → say less about less. OVERVIEW downscales the frame (×0.5); TRIM crops to a boxed region before describing.
- [ ] History is actions-only — no accumulation of verbose describe text — to keep the Brain's context tight.

---

## 5. Security

- [ ] Secrets live in a gitignored `.env` (`OPENROUTER_API_KEY`, one key for Brain + Eyes); only a value-free `.env.example` would ever be tracked. Never commit the key.
- [ ] The vision sandbox is a disposable container; Chrome runs `--no-sandbox` ONLY inside that container, never on the host.

---

## 6. Cleanup

- [ ] No emoji / non-ASCII in OUR OWN console output — Windows `cp1252` raises `UnicodeEncodeError` and crashes the script. Use ASCII markers (`PASS:` / `FAIL:` / `WARN:`). (Em-dashes inside data strings are fine; the console is reconfigured utf-8-safe for uncontrolled VLM text.) See [python-conventions.md](python-conventions.md).
- [ ] Runtime artifacts (`/artifacts/*`) are gitignored wholesale (regenerate via phase tests); written only through `paths.py` `artifact()`.
- [ ] Local-only binaries (`/tools/` — adb for PhoneDevice) are gitignored.

---

## 7. Consistency

- [ ] Python `snake_case`; LF line endings (convert edited lines to LF, leave untouched lines as-is).
- [ ] Module-per-role: `screen/` (Screen + Hands + shell), `eyes/`, `brain/`, `agent/` (the loop), `devices/`; single-purpose root utilities (`paths.py`, `runlog.py`, `structured.py`, `framediff.py`).
- [ ] Tests are co-located as `test_*.py` per module. Prefer MODEL-FREE / deterministic regression tests (`test_describe`, `test_hands`, `test_shell`, `/pointer`) over live-model tests; phase proofs are `test_phaseN.py`.

---

## 8. Tooling & Linter Configuration

- [ ] `[NEW]` Lint + format via **ruff** (`ruff check` / `ruff format`), config in [ruff.toml](ruff.toml), line length 99. Dev-only dependency in [requirements-dev.txt](requirements-dev.txt). (Added 2026-07-16 — the project previously had no linter.)
- [ ] Tests run as plain `python <module>/test_*.py` scripts (`if __name__ == "__main__"`), model-free where possible; no pytest harness required.
- [ ] Structured LLM output is guarded by **Pydantic validation** in [structured.py](structured.py) — the runtime enforcement of the format standard (Dimension 9).
- [ ] Dependencies split: agent-side in root [requirements.txt](requirements.txt); the Screen container in [screen/requirements.txt](screen/requirements.txt).
- [ ] `[NOT FOUND: no CI]` — tests + ruff are run locally. (CI deferred by choice, 2026-07-16.)

---

## 9. Project-Specific Conventions

- [ ] **★ FORMATS ARE ENFORCED BY OUR SCHEMA + VALIDATION, NOT THE AI.** Any output that must conform to a format is validated by us — never hand-formatted by the model, nor assembled as a hand-built string the model must get right. For LLM structured output: define a **Pydantic model → `response_format: json_schema` (strict:false) → validate through Pydantic** ([structured.py](structured.py), ADR-005). NO tool-calling; NEVER `response_format: {"type": "json_object"}` free-text for structured data. Validity must NOT depend on the provider enforcing the schema — OUR Pydantic validation is the guard (a hard grammar / forced tool-call also degenerates reasoning models, so we keep `strict:false`). *Enforced at runtime by `structured.py`; checked in review by `/analyze-code-quality` (Dimension 8).*
- [ ] ADRs live in `docs/adr/YYYY-MM-DD-slug.md`, numbered `ADR-00N`, and are referenced from the commit that introduces them.
- [ ] Commits follow **Conventional Commits with a scope**: `feat(eyes):`, `feat(agent):`, `chore(agent):`, etc., citing the ADR when one applies.
- [ ] Docs are localized in-repo (`docs/` + `.agents/`); the orientation map (`docs/orientation-map.md`) indexes them.
- [ ] OpenRouter model slugs are centralized as module constants (in `brain/`, `eyes/`) and swappable at call time (e.g. `run(brain_model=...)`).

---
