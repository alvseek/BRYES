# BRYES secrets — TEMPLATE ONLY (safe to commit; contains no real values).
#
# Setup:
#   1. Copy this file to .env   (the real .env is gitignored and never committed)
#   2. Paste your real key after the '=' below
#
# One OpenRouter key covers both the Eyes (UI-TARS) and the Brain (DeepSeek).
# Get it from https://openrouter.ai/keys
OPENROUTER_API_KEY=
