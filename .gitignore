# Proof artifacts (regenerate with the phase test scripts)
shot_*.png
desktop.png
eyes_*.png

# Secrets — never commit. Only .env.example (no real values) is tracked.
.env

# Python
__pycache__/
*.pyc
.venv/
