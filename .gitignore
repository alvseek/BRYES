# Phase 1 proof artifacts (regenerate with screen/test_phase1.py)
shot_*.png
desktop.png

# Secrets — never commit. Only .env.example (no real values) is tracked.
.env

# Python
__pycache__/
*.pyc
.venv/
