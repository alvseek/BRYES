# BRYES agent-side deps (the Brain/Eyes/Loop; the Screen container has its own in screen/).
pydantic>=2          # structured LLM output: schema definition + validation guard (ADR-005)
Pillow>=10           # the Eyes: screenshot decode, downscale, and crop
flask>=3             # the host task API (POST /tasks, GET /tasks/<id>) over run() (ADR-008)
