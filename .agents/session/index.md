# BRYES Session Log

Episodic work-product for BRYES — one file per theme, newest sub-episode first inside
each. Graduated from the fleet's central store per ADR-010.

| Theme | Agents | Summary |
|---|---|---|
| [bryes-vision-computer-use-agent.md](bryes-vision-computer-use-agent.md) | software-architect | The BRYES build, phase by phase. Latest (2026-07-17 15.41): **WhatsApp north-star push** — fixed **two masked, opposite coordinate bugs** (`locate`/UI-TARS = ABSOLUTE, removed a wrong rescale; `box`/Qwen = RESIZED-SPACE, restored+wired smart_resize) so taps land + crops center; **unified type_into to `text`** + rebuilt the prompt as an **action→fields spec**; built an **app-profile system** (`profiles/<os>/<app>/profile.md`, Visual→Eyes + Operating→Brain, incl. WhatsApp send=tap-button-not-Enter). Coordinate fixes validated (4M + phone); profile wired but **e2e unvalidated** (WhatsApp not yet sent — send-button + locked phone). Commit `3d3891c`. Prior (07-17 11.32): **`type_into` batched text-entry** — ONE Brain action doing the whole *[click? → clear? → type → Enter?]* gesture (a **device capability**). Also `OVERVIEW_SCALE 0.5→0.375`. Prior (07-17 10.33): housekeeping — container refresh + `/archive-old-memories`. (07-17 09.40): **json_schema mechanism (ADR-005 amended)** — deepseek primary + gemini backup, qwen dropped. (07-16 20.36): structured-output standard (ADR-005) + verify-focus. (07-16 13.53): foveal describe + trim (ADR-004) — describe-speed SOLVED (5–16s → ~2s). (07-16 09.48): Phase 5 verify-and-recover (ADR-003). Earlier: Device interface + real phone (ADR-002, 07-15); shell effector channel (ADR-001, 07-15); Hands + `wait`/`screenshot` → Tokopedia (07-14); VLM describe + atomic `type` + bake-off (07-13); Phases 1–4 + 1024 diagnosis (07-11). |
